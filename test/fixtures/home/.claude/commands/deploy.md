---
description: Ship the current branch — run checks, commit, push, and open a pull request for review
allowed-tools: Bash(git:*), Bash(gh:*)
---

Run the test suite, commit pending changes with a conventional message, push the branch, and open a PR against master.
